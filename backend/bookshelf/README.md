# Bookshelf Backend

For local development, copy `src/main/resources/application-example.properties` to `application.properties` and update the placeholder values with your own credentials. Keep sensitive values out of version control.
